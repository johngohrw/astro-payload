---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"contactWithTestimonial"> = block;

const {
  heading,
  description,
  submitLabel,
  privacyPolicyHref,
  formAction,
  logoLight,
  logoDark,
  testimonialQuote,
  testimonialAuthorName,
  testimonialAuthorRole,
  testimonialAuthorImage,
} = blockProps;

const remappedProps = {
  heading: heading ?? undefined,
  description: description ?? undefined,
  submitLabel: submitLabel ?? undefined,
  privacyPolicyHref: privacyPolicyHref ?? undefined,
  formAction: formAction ?? undefined,
  logoSrcLight: getImageUrl(logoLight),
  logoSrcDark: getImageUrl(logoDark),
  logoAlt: getImageAlt(logoLight),
  testimonialQuote: testimonialQuote ?? undefined,
  testimonialAuthorName: testimonialAuthorName ?? undefined,
  testimonialAuthorRole: testimonialAuthorRole ?? undefined,
  testimonialAuthorImageSrc:
    getImageUrl(testimonialAuthorImage),
  testimonialAuthorImageAlt:
    getImageAlt(testimonialAuthorImage),
};
---

<Component {...remappedProps} />

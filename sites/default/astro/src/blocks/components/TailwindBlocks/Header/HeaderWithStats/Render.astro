---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"headerWithStats"> = block;

const { heading, description, backgroundImage, links, stats } = blockProps;

const remappedProps = {
  heading: heading ?? undefined,
  description: description ?? undefined,
  backgroundImageSrc:
    getImageUrl(backgroundImage),
  backgroundImageAlt:
    getImageAlt(backgroundImage),
  links: links?.map((item) => ({
    label: item.label ?? undefined,
    href: item.href ?? undefined,
  })),
  stats: stats?.map((item) => ({
    value: item.value ?? undefined,
    label: item.label ?? undefined,
  })),
};
---

<Component {...remappedProps} />

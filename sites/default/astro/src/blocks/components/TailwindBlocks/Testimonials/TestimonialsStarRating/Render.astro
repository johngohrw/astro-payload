---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"testimonialsStarRating"> = block;

const { starCount, quote, authorImage, authorName, authorTitle } = blockProps;

const remappedProps = {
  starCount: starCount ?? undefined,
  quote: quote ?? undefined,
  authorImageSrc: getImageUrl(authorImage),
  authorName: authorName ?? undefined,
  authorTitle: authorTitle ?? undefined,
};
---

<Component {...remappedProps} />

---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"ctaWithImageTiles"> = block;

const { heading, descriptionPrimary, descriptionSecondary, ctaLabel, ctaHref, imageLarge, imageTopLeft, imageMiddle, imageBottomRight } = blockProps;

const remappedProps = {
  heading: heading ?? undefined,
  descriptionPrimary: descriptionPrimary ?? undefined,
  descriptionSecondary: descriptionSecondary ?? undefined,
  ctaLabel: ctaLabel ?? undefined,
  ctaHref: ctaHref ?? undefined,
  imageLargeSrc: getImageUrl(imageLarge),
  imageLargeAlt: getImageAlt(imageLarge),
  imageTopLeftSrc: getImageUrl(imageTopLeft),
  imageTopLeftAlt: getImageAlt(imageTopLeft),
  imageMiddleSrc: getImageUrl(imageMiddle),
  imageMiddleAlt: getImageAlt(imageMiddle),
  imageBottomRightSrc: getImageUrl(imageBottomRight),
  imageBottomRightAlt: getImageAlt(imageBottomRight),
};
---

<Component {...remappedProps} />

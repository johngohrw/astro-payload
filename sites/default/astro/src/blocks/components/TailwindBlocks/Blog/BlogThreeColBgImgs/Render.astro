---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"blogThreeColBgImgs"> = block;

const { heading, description, posts } = blockProps;

const remappedProps = {
  heading: heading ?? undefined,
  description: description ?? undefined,
  posts: posts?.map((post) => ({
    backgroundImageSrc:
      getImageUrl(post.backgroundImage),
    backgroundImageAlt:
      getImageAlt(post.backgroundImage),
    date: post.date ?? undefined,
    dateTime: post.dateTime ?? undefined,
    title: post.title ?? undefined,
    titleHref: post.titleHref ?? undefined,
    author: post.author && post.author !== null
      ? {
          name: post.author.name ?? undefined,
          imageSrc: getImageUrl(post.author.image),
          imageAlt: getImageAlt(post.author.image),
        }
      : undefined,
  })),
};
---

<Component {...remappedProps} />

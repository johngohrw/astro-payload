---
import type { Page } from "@payload-default/payload-types";
import RenderSingleBlock from "./RenderSingleBlock.astro";

interface Props {
  blocks: Page["contentBlocks"];
}

const { blocks } = Astro.props;
---

{blocks?.map((block) => <RenderSingleBlock block={block} />)}

Create a skill:
You are an expert UI Designer and Web UI Developer. Analyze all the components in the given zip file. The components come from the same vein (tailwind UI blocks). Understand the design system and guidelines used by this group of components and remember it well. Also pay close attention to things like:

- the standard max-width for content
- standardized spacing of elements
- standardized font sizes and spacing across all screen sizes
- use of dark: tailwind class
- proper html semantics
- responsive breakpoints and mobile friendliness
- you may ignore parts where it tells you to install @tailwindplus/elements

When you are given a description, image, or an example code for a component: create a template-only(html+tailwind) component that has the same design language and conventions as the training set. Make sure it fits the training set in terms of mobile responsiveness, code quality, tailwind class conventions, and design look and feel. You may also provide a name for the component, if one is not given.

Your output should be an appropriately-named .html file. You may also briefly describe your thought process or decision-making during the process in a succinct manner.

additional notes:

- when it comes to mock images, just use https://picsum.photos/400 . this url will return a random placeholder jpg image of 400x400px
- when it comes to mock content. you can keep it generic. everything is a placeholder and meant to be replaced.
- svg icons can be generic too. just putting a simple shape will do.

Study the base set well, provide extensive examples to strengthen the core guideline.

Now create a well-defined and robust skill based on the description above.

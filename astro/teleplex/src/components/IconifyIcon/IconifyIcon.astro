---
// this component is a perfect example of an astro component that works
// exactly like another astro component, complete with types
import Component from "@astro-default/components/IconifyIcon/IconifyIcon.astro";

type Props = Parameters<typeof Component>[0];

const { ...restProps } = Astro.props as Props;
---

<Component {...restProps as any} />

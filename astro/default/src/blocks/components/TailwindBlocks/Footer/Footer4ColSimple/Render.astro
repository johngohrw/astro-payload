---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"footer4ColSimple"> = block;

const { logoSrc, logoDarkSrc, logoAlt, columns } = blockProps;

const remappedProps = {
  logoSrc: getImageUrl(logoSrc),
  logoDarkSrc: getImageUrl(logoDarkSrc),
  logoAlt: logoAlt ?? undefined,
  columns: columns?.map((col) => ({
    heading: col.heading ?? "",
    links: col.links?.map((link) => ({
      label: link.label ?? "",
      href: link.href ?? "",
    })) ?? [],
  })),
};
---

<Component {...remappedProps} />

---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import Component from "./Component.astro";

const { block } = Astro.props;
const blockProps: BlockTypeOf<"statsSplitWithImg"> = block;
const { image, imageAlt, eyebrow, heading, description, stats } = blockProps;
const remappedProps = {
  imageSrc: typeof image === "object" && image !== null ? image.url ?? undefined : undefined,
  imageAlt: imageAlt ?? undefined,
  eyebrow: eyebrow ?? undefined,
  heading: heading ?? undefined,
  description: description ?? undefined,
  stats: stats?.map((stat) => ({
    label: stat.label ?? undefined,
    value: stat.value ?? undefined,
  })),
};
---
<Component {...remappedProps} />

---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";
const { block } = Astro.props;
const blockProps: BlockTypeOf<"featWithProductScreenshotPanel"> = block;
const { eyebrow, heading, description, features, screenshot } = blockProps;
const remappedProps = {
  eyebrow: eyebrow ?? undefined,
  heading: heading ?? undefined,
  description: description ?? undefined,
  features: features?.map((f) => ({ title: f.title ?? "", description: f.description ?? "", iconSvg: f.iconSvg ?? "" })),
  screenshotSrc: getImageUrl(screenshot),
  screenshotAlt: getImageAlt(screenshot),
};
---
<Component {...remappedProps} />

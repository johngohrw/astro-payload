---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"ctaTwoColumnsWithPhoto"> = block;

const { heading, description, benefits, ctaLabel, ctaHref, image } = blockProps;

const remappedProps = {
  heading: heading ?? undefined,
  description: description ?? undefined,
  benefits: benefits?.map((item) => ({
    iconSvg: item.iconSvg ?? "",
    label: item.label ?? "",
  })),
  ctaLabel: ctaLabel ?? undefined,
  ctaHref: ctaHref ?? undefined,
  imageSrc: getImageUrl(image),
  imageAlt: getImageAlt(image),
};
---

<Component {...remappedProps} />

---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import { getImageUrl, getImageAlt } from "@astro-default/utils/media";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"testimonialsSideBySide"> = block;

const { testimonials } = blockProps;

const remappedProps = {
  testimonials: testimonials?.map((item) => ({
    logoSrc: getImageUrl(item.logo),
    logoAlt: getImageAlt(item.logo),
    quote: item.quote ?? "",
    authorImageSrc: getImageUrl(item.authorImage),
    authorName: item.authorName ?? "",
    authorTitle: item.authorTitle ?? "",
  })),
};
---

<Component {...remappedProps} />

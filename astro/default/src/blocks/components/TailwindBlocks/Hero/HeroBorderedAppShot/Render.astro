---
import type { BlockTypeOf } from "@/blocks/blockTypes";
import Component from "./Component.astro";

const { block } = Astro.props;

const blockProps: BlockTypeOf<"heroBorderedAppShot"> = block;

const { logo, navLinks, login, heading, description, screenshot, primaryCta, secondaryCta } = blockProps;

const remappedProps = {
  logoSrc: typeof logo === "object" && logo !== null ? logo.url ?? undefined : undefined,
  logoSrcDark: typeof logo === "object" && logo !== null ? logo.url ?? undefined : undefined,
  logoAlt: typeof logo === "object" && logo !== null ? logo.alt ?? undefined : undefined,
  navLinks: navLinks?.map((link) => ({
    label: link.label ?? undefined,
    href: link.href ?? undefined,
  })),
  loginLabel: login?.label ?? undefined,
  loginHref: login?.href ?? undefined,
  heading: heading ?? undefined,
  description: description ?? undefined,
  screenshotSrc: typeof screenshot === "object" && screenshot !== null ? screenshot.url ?? undefined : undefined,
  screenshotSrcDark: typeof screenshot === "object" && screenshot !== null ? screenshot.url ?? undefined : undefined,
  screenshotAlt: typeof screenshot === "object" && screenshot !== null ? screenshot.alt ?? undefined : undefined,
  primaryCta: primaryCta && primaryCta !== null
    ? { label: primaryCta.label ?? undefined, href: primaryCta.href ?? undefined }
    : undefined,
  secondaryCta: secondaryCta && secondaryCta !== null
    ? { label: secondaryCta.label ?? undefined, href: secondaryCta.href ?? undefined }
    : undefined,
};
---

<Component {...remappedProps} />

---
import { type BlockType } from "../../blocks/_blockTypes";
import { allBlockRenderersMap } from "../../blocks/rendererMap";

interface Props {
  block: BlockType;
}

const { block } = Astro.props;

const Renderer = allBlockRenderersMap[block.blockType];
---

{Renderer && <Renderer block={block} />}
